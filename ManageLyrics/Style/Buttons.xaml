<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:ManageLyrics">
    
    <ResourceDictionary.MergedDictionaries>
        <!-- x:Key="IconButton"  order is imfortant -->
        <ResourceDictionary Source="Colors.xaml"/>
        <ResourceDictionary Source="Fonts.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <Style TargetType="{x:Type Button}"  BasedOn="{StaticResource DarkButton}">
    </Style>

    <Style x:Key="ButtonIcon" TargetType="{x:Type Button}" BasedOn="{StaticResource DarkButton}">
        <Setter Property="FontSize" Value="{StaticResource FontSizeRegular}" />
        <Setter Property="FontFamily" Value="{StaticResource FontAwesome}" />
        <Setter Property="Height" Value="40" />
        <Setter Property="Width" Value="80" />
    </Style>

</ResourceDictionary>
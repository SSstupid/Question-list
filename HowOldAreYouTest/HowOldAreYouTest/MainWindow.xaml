<Window x:Class="HowOldAreYouTest.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HowOldAreYouTest"
        mc:Ignorable="d"
        Title="MainWindow" Height="300" Width="450">
    
    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Orientation="Horizontal" >
            <TextBlock Text="나이를 설정하세요"/>
            <TextBox Text="{Binding GetSetAge}" Width="50" Margin="10,0,0,0"/>
        </StackPanel>
        
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <TextBlock Text="나이 : "/>
            <TextBox  Text="{Binding GetSetAge}" Width="50" Margin="0,0,0,0"/>
        </StackPanel>

    </Grid>
</Window>
